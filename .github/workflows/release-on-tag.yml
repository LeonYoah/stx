# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Release on Tag

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  package-release:
    name: Package split release assets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'
          cache: maven

      - name: Build split release assets
        env:
          APP_VERSION: ${{ github.ref_name }}
        run: |
          chmod +x scripts/package-release.sh scripts/install-online.sh scripts/download-bundle.sh
          scripts/package-release.sh \
            --arch all \
            --layout split \
            --build-frontend \
            --emit-deps \
            --node-major 22 \
            --node-variant official \
            --version "${APP_VERSION}"
          # glibc217 unofficial builds are x64-only.
          # glibc217 非官方构建目前只有 x64。
          scripts/package-release.sh \
            --arch amd64 \
            --layout split \
            --emit-deps \
            --node-major 18 \
            --node-variant glibc217 \
            --version "${APP_VERSION}"

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: stx-packages-${{ github.ref_name }}
          path: dist/releases/**
          if-no-files-found: error
          retention-days: 30

  publish-github-release:
    name: Publish GitHub Release Assets
    runs-on: ubuntu-latest
    needs: package-release

    steps:
      - name: Download package artifacts
        uses: actions/download-artifact@v4
        with:
          name: stx-packages-${{ github.ref_name }}
          path: release-assets

      - name: Show release assets
        run: ls -alh release-assets

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-assets/stx-linux-*
            release-assets/stx-agent-linux-*
            release-assets/frontend-standalone-*.tar.gz
            release-assets/frontend-standalone-*.tar.gz.sha256
            release-assets/node-*.tar.gz
            release-assets/node-*.tar.gz.sha256
            release-assets/observability-*.tar.gz
            release-assets/observability-*.tar.gz.sha256
            release-assets/SHA256SUMS
            release-assets/MANIFEST.json
            release-assets/install-online.sh
            release-assets/download-bundle.sh
            release-assets/*.sha256
          generate_release_notes: true
          fail_on_unmatched_files: false

  publish-ghcr-images:
    name: Publish GHCR ${{ matrix.image_name }}
    runs-on: ubuntu-latest
    needs: package-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - image_name: stx-backend
            dockerfile: .github/docker/Dockerfile.backend
          - image_name: stx-frontend
            dockerfile: .github/docker/Dockerfile.frontend
          - image_name: stx-all-in-one
            dockerfile: .github/docker/Dockerfile.all-in-one

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lowercase owner
        id: owner
        run: echo "value=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.owner.outputs.value }}/${{ matrix.image_name }}
          tags: |
            type=ref,event=tag
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: true
          provenance: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
