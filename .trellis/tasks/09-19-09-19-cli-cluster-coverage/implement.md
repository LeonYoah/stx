# 实施步骤

## 1. 前置检查

- [x] 读取 Trellis 继续开发规则和 backend CLI、安全、质量规范。
- [x] 确认当前分支和无关前端改动。
- [x] 调查 cluster 路由、请求结构、操作登记和现有写命令公共实现。

## 2. 批量实现

- [x] 登记 cluster 缺失操作，补齐影响说明、参数和输出样例。
- [x] 增加集群和节点专用写命令，复用统一确认、幂等和输出实现。
- [x] 将可安全生成的 GET、无正文 POST/DELETE 接入生成命令。
- [x] 增加命令树、请求正文、请求头和错误处理测试。
- [x] 更新路由基线与操作登记测试。

## 3. 自动检查

- [x] 格式化新增或修改的 Go 文件。
- [x] 运行目标包测试、路由契约报告和完整 Go 测试。
- [x] 运行 `go vet ./...`、数据库兼容脚本、License 检查和 `git diff --check`。
- [x] 构建 `dist/stx`。

## 4. 真实验证

- [x] 使用软连接执行帮助、查询和输出格式验证。
- [x] 创建临时集群并完成更新、节点写操作和删除。
- [x] 验证确认 Header，并发现服务端尚未实现 cluster 幂等重放。
- [x] 在安全测试资源上验证空集群启停重启和 Java Proxy 启停重启，记录影响提示。
- [x] 清理临时资源并确认无残留。

## 5. 提交

- [x] 记录真实验证结果。
- [ ] 只暂存本任务文件和 Trellis 记录，不包含无关前端改动。
- [ ] 使用中文提交信息提交。

## 回撤

不自动回撤。需要回撤时先说明文件和提交范围并取得用户确认，禁止使用 `git restore`。
