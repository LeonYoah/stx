# source · LocalFile JSON
# curated-section: source | LocalFile

LocalFile {
  plugin_output = "file_src"
  path = "/data/seatunnel/inbox"
  file_format_type = "json"
  schema = {
    fields {
      id = "bigint"
      name = "string"
      status = "string"
    }
  }
}
