# transform · Copy（字段拷贝示例）
# curated-section: transform | Copy
# plugin_input / plugin_output 需与上下游对齐后改名

Copy {
  plugin_input = "src"
  plugin_output = "staged"
  fields {
    copied_name = name
  }
}
