# source · MySQL-CDC 单表
# curated-section: source | MySQL-CDC | single

MySQL-CDC {
  plugin_output = "cdc"
  url = "jdbc:mysql://127.0.0.1:3306/seatunnel_demo"
  username = "cdc_user"
  password = "{{cdc_password}}"
  table-names = ["seatunnel_demo.users"]
  startup.mode = "initial"
}
