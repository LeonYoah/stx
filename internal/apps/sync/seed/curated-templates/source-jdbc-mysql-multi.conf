# source · JDBC MySQL 多表
# curated-section: source | Jdbc | multi

Jdbc {
  plugin_output = "multi_src"
  url = "jdbc:mysql://127.0.0.1:3306/seatunnel_demo"
  driver = "com.mysql.cj.jdbc.Driver"
  username = "root"
  password = "{{jdbc_password}}"
  table_list = [
    { table_path = "seatunnel_demo.orders" }
    { table_path = "seatunnel_demo.order_items" }
    { table_path = "seatunnel_demo.users" }
  ]
}
