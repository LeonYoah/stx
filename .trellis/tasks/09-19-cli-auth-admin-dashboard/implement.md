# 实施步骤

## 1. 准备

- [x] 读取适用的 backend 和通用开发规范。
- [x] 检查 auth、admin、dashboard handler 的真实请求和响应。
- [x] 记录并避开工作区内与本任务无关的修改。

## 2. 增加操作登记

- [x] 在 `internal/operation/registry.go` 增加 8 个只读操作。
- [x] 为 admin 用户列表登记真实 query 参数。
- [x] 为每项操作补充命令示例和输出样例。

## 3. 增加测试

- [x] 扩充 operation 登记测试，检查操作数量、路径、管理员限制和参数。
- [x] 扩充根命令测试，检查新的命令树。
- [x] 更新路由契约基线，历史缺口只减少本批 8 项。

## 4. 自动验证

- [x] 对修改的 Go 文件执行 `gofmt`。
- [x] 运行目标包测试和路由契约检查。
- [x] 运行 `go test ./...`、`go vet ./...` 和 `git diff --check`。
- [x] 构建 `dist/stx` 并记录架构与 SHA-256。

## 5. 本地真实服务验证

- [x] 使用最新二进制重启本机 STX 服务，确认 Agent 重新注册。
- [x] 使用隔离的 CLI 配置登录，不在输出中显示密码和令牌。
- [x] 执行 8 个新增命令。
- [x] 验证 JSON、YAML、table、`--pick`、管理员 capability 和未找到退出码。
- [x] 验证完成后退出登录。

## 6. 检查和提交

- [x] 使用 `trellis-check` 检查需求、实现、测试和真实验证记录。
- [x] 判断现有 CLI 规范是否需要补充；本批沿用已有登记生成 GET 命令约定，无需修改规范。
- [ ] 只暂存本任务文件并使用中文提交信息提交。

## 可同时进行的检查

- 操作登记测试和根命令测试可以分别检查。
- 目标包测试、路由契约检查和静态检查可在代码完成后同时运行。
- 真实服务验证必须使用已经通过测试的最新二进制。

## 回退说明

不自动回退。需要回退时先向用户说明影响并取得确认，禁止执行 `git restore`。

## 验证结果

- 日期：2026-09-19。
- 路由统计：246 条路由、114 个 Swagger 操作、25 个登记操作、19 个特殊例外、202 个历史缺口。
- 目标包测试、`go test ./...`、`go vet ./...`、路由契约检查和 `git diff --check` 均通过。
- 最终二进制：`dist/stx`，darwin/arm64，SHA-256 为 `2a3ad566f294f0b71bb175f635f2f57500ad862ce32d2049605733500ebc67f9`。
- 最新二进制已启动本机 HTTP `17800` 和 gRPC `17890`；Dashboard 返回 1 台在线主机、1 个运行集群和 1 个在线 Agent。
- 管理员真实执行 8 个新增命令全部成功；用户列表的分页、用户名、启用状态和管理员状态筛选均生效。
- JSON、YAML、table 和 `--pick` 输出均通过；不存在用户返回退出码 5，缺少必填分页参数返回退出码 2。
- 临时创建非管理员用户后，两个 admin capability 均返回 `admin_required`，执行 admin 命令返回退出码 4；验证后已删除临时用户。
- 三个用户信息结果已检查，未出现密码、令牌、密钥或哈希字段；隔离配置文件权限为 `0600`，验证后已退出登录。
