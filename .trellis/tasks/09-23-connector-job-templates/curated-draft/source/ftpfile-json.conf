# source · FtpFile JSON
# curated-section: source | FtpFile

FtpFile {
  plugin_output = "file_src"
  host = "192.168.1.100"
  port = 21
  user = "ftpuser"
  password = "{{ftp_password}}"
  path = "/data/inbox"
  connection_mode = "passive_local"
  file_format_type = "json"
  schema = {
    fields {
      id = "bigint"
      name = "string"
      status = "string"
    }
  }
}
