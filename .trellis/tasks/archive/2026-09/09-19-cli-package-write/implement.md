# 安装包管理 CLI 写操作覆盖实施计划

## 1. 任务启动前检查

- [x] 读取 backend、CLI、执行安全、错误处理、质量和 shared guides 规范。
- [x] 检查当前分支和无关未提交文件，限定本任务改动范围。
- [x] 确认本地 STX 服务、软连接、登录配置和 Agent 状态。
- [x] 检查安装包服务的上传校验、分片状态、下载 goroutine、取消信号和错误映射。

## 2. 客户端与命令实现

- [x] 增加流式 multipart 客户端方法和定向测试。
- [x] 增加统一写操作选项、确认 Header、幂等键和警告输出。
- [x] 登记版本刷新和主机安装命令查询，或接入已有登记项。
- [x] 实现 package 上传和分片上传命令。
- [x] 实现 package 下载、取消和删除命令。
- [x] 保证 package 根命令只注册一次，并补充帮助、样例和输出样例。

## 3. 服务端保护和取消修正

- [x] 为本批写接口接入最小服务端确认和幂等校验。
- [x] 修正下载取消，使取消信号真实传递到下载执行过程。
- [x] 补充取消后的临时文件清理和状态不倒退测试。
- [x] 补充直接 API 调用未确认、幂等重复和幂等冲突测试。

## 4. 质量检查

- [x] `gofmt` 修改过的 Go 文件。
- [x] 运行 CLI、client、installer、execution 定向测试。
- [x] 运行 `go test ./...`。
- [x] 运行 `go vet ./...`。
- [x] 运行操作登记合约报告并检查路由覆盖变化。
- [x] 对本任务文件运行 `git diff --check`，不检查或修改无关安装发布文件。

## 5. 本地真实验证

- [x] 构建 `dist/stx`，确认 `/Users/mac/.local/bin/stx` 仍指向它。
- [x] 重启本地服务并确认 capability 包含本批操作。
- [x] 用隔离配置登录本地服务。
- [x] 刷新版本并调用主机安装命令查询。
- [x] 使用测试版本完成单文件上传、查询和删除。
- [x] 使用测试版本完成分片上传、查询和删除。
- [x] 启动下载并在可控时间内取消，检查任务状态和临时文件。
- [x] 验证确认缺失、`--confirm`、幂等重试、幂等冲突、JSON/table/raw/`--pick` 和稳定退出码。

## 6. 提交与归档

- [x] 使用 `trellis-check` 完成最终检查。
- [x] 如发现长期适用的新 CLI 规则，更新对应 spec；否则不修改规范文件。
- [ ] 只暂存本任务相关文件并使用中文提交信息。
- [x] 记录真实验证结果和未覆盖项。
- [ ] 归档本任务并记录开发日志。

## 7. 真实验证记录

- 使用 2026-09-19 构建的 `dist/stx` 和 `/Users/mac/.local/bin/stx` 软连接连接 `http://127.0.0.1:17800`。
- 隔离命名空间 `package-e2e` 登录成功，当前用户为管理员 `admin`，令牌默认有效期 7 天。
- 7 个接口都已通过真实命令调用；安装包相关路由均已登记，且 Swagger 已包含对应操作。
- 单文件上传版本 `9.9.91`、分片上传版本 `9.9.92` 成功，随后均通过 CLI 删除；原有 `2.3.13` 安装包仍存在。
- 下载版本 `2.3.12` 后立即取消，HTTP 请求实际停止，临时文件和最终文件均未留下，业务任务和公共执行记录最终均为 `cancelled`。
- 不带 `--confirm` 时 CLI 不发业务请求并返回退出码 6；直接 API 缺少确认时返回 HTTP 428。
- 同一幂等键和同一请求会复用原执行；同一幂等键改版本会返回冲突。补充内容摘要后，同名同大小但内容不同的文件也会返回幂等冲突，退出码 6。
- JSON、table、YAML、raw 和 `--pick` 均已通过真实输出验证；warning 写入 stderr，结果写入 stdout。
- 审计表中可以查询到 package 上传、分片上传、下载、取消和删除的创建与结果记录，CLI 来源记录为 `client_type=cli`。
- `go test -race ./internal/apps/installer ./internal/apps/execution`、定向测试、`go test ./...`、`go vet ./...`、构建、路由合约报告和 `git diff --check` 均通过。
- 本批没有新增 HTTP 路由，原有 7 条路由已经有 Swagger 注释，因此没有改写 Swagger 生成文件。
